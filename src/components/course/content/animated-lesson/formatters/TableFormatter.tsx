import React from 'react';

// This function processes tables for text-based formatters
export const formatTables = (text: string) => {
  // For now, just return the text as-is since we handle tables in ContentFormatter
  return text;
};